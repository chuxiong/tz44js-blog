extends ./layout/layout

block css
  link(rel="stylesheet", href="/css/index.css")
//- 
  artList
    头像
    tips
    文章url
    作者
    时间
    评论数

block contents
  include ./layout/tag
  
  .layui-container
    .layui-row.layui-col-space15
      //- 文章列表
      .layui-col-md8
        ul.article-list
          li index.html
          li 这是 pug 渲染出来的首页
          //- each info in artList
          //-   li
          //-     a.list-face
          //-       img(src=info.author.avatar, alt=info.author)
          //-     h2
          //-       a.layui-badge= info.tips
          //-       a.articlt-title.ellipsis(href=`/article/${info._id}`)= info.title
          //-     .list-info
          //-       a= info.author.username
          //-       span= info.updated.toLocaleString()
          //-       span.list-reply
          //-         i(title="评论").layui-icon.layui-icon-file-b
          //-         = info.commentNum
        include ./layout/laypage.pug
        
      //- 右侧公告
      include ./layout/right_tag

block scripts
  script(src="/js/index.js")
  
    
    
    
    


